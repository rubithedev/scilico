# Scilico

## Requirements

`python`: ^3.14

`pip`: ^26.1

## Running the project

1. Install init venv:

```bash
python3 -m venv .venv &&  source .venv/bin/activate
```

2. Install dependencies

```bash
pip install -r requirements.txt
```

3. Run the `src/main.py`

```bash
python3 src/main.py
```
